-  How to run Js File In terminal
    - Just in terminal write - node file.js


- require('package_name')
    - for install package 'npm i package name';
    - call - require('package_name')

- package.json works
    - Carries user js code depends package details and version also.
- node_modules folder
    - It was carries install packages code.
- package-lock.jon
    - If user use a package, that package depends on another package and that package depends on another, This things maintance package-lock.jon


- Server Creation for backend
    - npm init -y
    - Express Entry Now
        - npm i express
        - In .js require('express') & call (express) - create server
        - app.listen(3000{portNo}) - start server
